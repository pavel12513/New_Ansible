---
- name: Config nginx ans static files
  block:
      tasks:
        - name: update nginx config
          template:
            src: templates/nginx.conf.j2
            dest: /etc/nginx/nginx.conf
            owner: nginx
          notify:
            - reload nginx
